export declare function normalizeAmplitudeSimple({ samplesData, channelsCount, channels, separateChannels, coef, }: {
    samplesData: Float32Array;
    channelsCount: number;
    channels?: number[];
    separateChannels?: boolean;
    coef: number;
}): void;
