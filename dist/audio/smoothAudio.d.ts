export declare function smoothAudio({ samplesData, channelsCount, channels, startSamples, endSamples, }: {
    samplesData: Float32Array;
    channelsCount: number;
    channels?: number[];
    startSamples: number;
    endSamples: number;
}): void;
